# NaijaLedger web

React + Vite + TypeScript frontend.

## Setup

```bash
pnpm install
```

## Dev

```bash
pnpm dev
# Engine API expected at VITE_API_BASE_URL (default http://localhost:8000)
```

## OpenAPI types

With the engine running:

```bash
make generate-api
```
